# notas-atletas
Algoritmo em JavaScript para calcular a média de notas de atletas, eliminando a maior e a menor nota. Esse código usa de manipulação de arrays e objetos em JS. Utiliza sort(), slice() e reduce() para processar médias com exclusão de outliers.
